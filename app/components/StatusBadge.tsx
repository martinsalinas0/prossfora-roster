const StatusBadge = () => {
  return (
    <div>
      StatusBadge
      <div>
        components/StatusBadge.tsx — one component, maps status strings to
        colors. Handles all nine job statuses plus quote/invoice/payment states.
      </div>
    </div>
  );
};

export default StatusBadge;
